

unsigned char crc8(const unsigned char *buf, size_t len, unsigned char prev);
unsigned short crc16(const unsigned char *buf, size_t len, unsigned short prev);
unsigned int crc32(const unsigned char *buf, size_t len, unsigned int prev);
